{% extends 'base.html.twig' %}


{% block body %}

{#
<div class="container-fluid divmt" >
    <div class="row">
        <div class="col-lg-5">
        </div>
        <div class="col-lg-4">
            <h1>Héros</h1>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-2">
        </div>
        <div class="col-lg-8">

            <table class="table">
                <thead class="thead-dark">
                    <tr>
                        <th>#</th>
                        <th>Name</th>
                        <th>Classe</th>
                        <th>Level</th>
                        <th>Health</th>
                        <th>Strength</th>
                        <th>Agility</th>
                        <th>Intelligence</th>
                        <th>Chance</th>
                        <th>Damages</th>
                        <th></th>
                    </tr>
                </thead>


                <tbody>
                {% set nb=0 %}
                {% for hero in heroes %}
                    {% if hero.user == app.user %}
                        {% set nb=nb+1 %}
                        <tr>
                            <td><a href="{{ path('hero_show', { 'id': hero.id }) }}">{{ nb }}</a></td>
                            <td>{{ hero.name }}</td>
                            <td>{{ hero.classe }}</td>
                            <td>{{ hero.level }}</td>
                            <td>{{ hero.health }}</td>
                            <td>{{ hero.strength }}</td>
                            <td>{{ hero.agility }}</td>
                            <td>{{ hero.intelligence }}</td>
                            <td>{{ hero.chance }}</td>
                            <td>{{ hero.damages }}</td>

                            <td>
                                <div class="">
                                    #}
{#<a class="btn btn-outline-danger btn-block" href="{{ path('hero_show', { 'id': hero.id }) }}">show</a>#}{#

                                    <a class="btn btn-outline-danger btn-block" href="{{ path('hero_edit', { 'id': hero.id }) }}">Modifier</a>
                                    <a class="btn btn-outline-warning btn-block" href="{{ path('level_index', { 'id': hero.id }) }}">Jouer</a>
                                </div>
                            </td>
                        </tr>
                    {% endif %}
                {% endfor %}
                </tbody>
            </table>
        </div>

        <div class="col-lg-2 ">
            <a class="btn btn-outline-primary" href="{{ path('hero_new') }}">Create a new hero</a>
        </div>

    </div>
</div>
#}

<div class="divmt">
{% set nb=0 %}
    {% for hero in heroes %}
        {% if hero.user == app.user %}
            {% set nb=nb+1 %}
                <div class="container">
                    <div class="row">

                        <div class=" col-md-6 mb-3 mt-3">
                            <img class="card-img-top" src="../../img/{{ hero.classe }}.jpg">
                        </div>

                        <div class="col-md-6 mb-3">
                            <h1  style="text-align: center;">{{ hero.name }}</h1>
                            <table class="table">
                                <tbody>
                                <tr>
                                    <th>Classe</th>
                                    <td>{{ hero.classe }}</td>
                                </tr>
                                <tr>
                                    <th>Niveau</th>
                                    <td>{{ hero.level }}</td>
                                </tr>
                                <tr>
                                    <th>Vitalité</th>
                                    <td>{{ hero.health }}</td>
                                </tr>
                                <tr>
                                    <th>Force</th>
                                    <td>{{ hero.strength }}</td>
                                </tr>
                                <tr>
                                    <th>Agilité</th>
                                    <td>{{ hero.agility }}</td>
                                </tr>
                                <tr>
                                    <th>Intelligence</th>
                                    <td>{{ hero.intelligence }}</td>
                                </tr>
                                <tr>
                                    <th>Chance</th>
                                    <td>{{ hero.chance }}</td>
                                </tr>
                                <tr>
                                    <th>Dégats</th>
                                    <td>{{ hero.damages }}</td>
                                </tr>
                                </tbody>
                            </table>
                            <a class="btn btn-outline-green" href="{{ path('level_index', { 'id': hero.id }) }}">Jouer</a>
                            <a class="btn btn-outline-danger" href="{{ path('hero_edit', { 'id': hero.id }) }}">Modifier</a>
                        </div>

                    </div>
                    <hr>
                </div>
        {% endif %}
    {% endfor %}
</div>
{% endblock %}
